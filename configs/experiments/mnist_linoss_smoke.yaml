defaults:
  - ../base
  - ../datasets/mnist
  - ../models/linoss

name: mnist-linoss-smoke
description: Smoke experiment for the packaged MNIST training pipeline.
tags:
  - smoke
  - mnist
  - linoss
trainer:
  num_epochs: 1
  max_steps: 2
  log_every_steps: 1
  eval_every_steps: 1
  checkpoint_every_steps: 1
wandb:
  tags:
    - smoke
