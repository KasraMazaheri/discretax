dataset:
  kind: weather
  name: weather
  root: processed/Weather
  download: false
  sequence_layout: time_major
  normalize: false
  params:
    loss_window: 720
    regression_loss: mse
trainer:
  num_epochs: 10
  max_steps: 25000
  eval_every_steps: 200
